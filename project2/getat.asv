function [ val ] = getat( image, coords )
coords = round(coords);
r = coords(1);
c = coords(2);
l = coords(3);

maxr = size(image,1);
maxc = size(image,2);
maxl = size(image,3);

if r < 1 || r > maxr || c < 1 || c > maxc || l < 1 || l > maxl
    val = 0;
else 
    val = image
end

